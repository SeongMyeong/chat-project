// const daemon = require("./daemon");

// const chatConfig = {
//   port: 4001,
//   namespace: "chat-space",
// };

// const nsp = daemon.io.of(chatConfig);

// nsp.on("connection", (socket) => {});

// daemon.server.listen(chatConfig.port, () => {
//   console.log(`[masonms] chat server listen port ${chatConfig.port}`);
// });
